import 'package:flutter/material.dart';

class SellNFTPage extends StatelessWidget {
  const SellNFTPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rao bán NFT')),
      body: const Center(child: Text('Trang rao bán NFT (chưa triển khai)')),
    );
  }
}
