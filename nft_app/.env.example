# Example `.env` for local development (DO NOT commit secrets to VCS)
#
# How values are resolved in this project:
# 1) Compile-time `--dart-define` values (recommended for CI / production)
# 2) Values from this `.env` file (loaded in `main()` via `flutter_dotenv`) — convenient for local dev
# 3) Hard-coded defaults in `lib/src/config/environment.dart`
#
# Usage (development): copy this file to `.env`, fill values, then run:
#   flutter pub get
#   flutter run
#
# Usage (CI / production): prefer `--dart-define` so values are injected at build-time:
#   flutter build apk --release --dart-define=RPC_URL=https://... --dart-define=CONTRACT_ADDRESS=0x...
#
# WARNING: Never put production secrets (API secrets, private keys) inside mobile app builds.
# If you must use Pinata or other secret APIs, implement a backend proxy that holds secrets server-side.

# RPC node (Alchemy / Infura / public RPC)
RPC_URL=https://eth-sepolia.g.alchemy.com/v2/FkCaghR4VF2YJjv_eOzfA

# WalletConnect project id (public)
WALLETCONNECT_PROJECT_ID=c7950190937e58bbb9cf5c4e0381f56a

# Contract address (Sepolia)
CONTRACT_ADDRESS=0x994EbecB94269902D99cF4b69824B78B5627f19A

# Pinata (dev only) - do NOT put these in production mobile builds
# địa chỉ  cũ
PINATA_API_KEY=40bf29eab173bda4dcc4
PINATA_SECRET=a2981c985fbfe75ccaf6a39e0681cb3107f61e9f60a5c1d452b4ad7bcaa29ece
# địa chỉ  mới
PINATA_API_KEY=894e1d04ea50ef737720
PINATA_SECRET=9901fa3f82f992c4cfb0fe7b9764f834f82d89e4ec17d9344ffb40e58c7c626a